"use client";

interface ChatProps {
  viewerName: string;
  hostName: string;
  hostIdentity: string;
}

function Chat({ viewerName, hostName, hostIdentity }: ChatProps) {
  return <div>Chat</div>;
}

export default Chat;
